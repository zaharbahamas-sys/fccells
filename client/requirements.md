## Packages
recharts | Data visualization for power loads and comparisons
framer-motion | Smooth dashboard transitions and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The application requires a technical, data-dense dashboard aesthetic.
Authentication is currently open (no login required for MVP).
API endpoints follow strict REST patterns defined in shared/routes.ts.
